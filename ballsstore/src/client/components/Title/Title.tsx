import React from 'react';
import { StyledTitle } from './styled';

const Title = () => {
   return (
      <StyledTitle>
         New Releases Books
      </StyledTitle>
   );
};

export default Title;