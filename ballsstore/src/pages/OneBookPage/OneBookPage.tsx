import React, { FC } from 'react';
import { OneBookWrapper } from './styles';

interface IOneBookProps {
   id: string;
}

const OneBookPage: FC<IOneBookProps> = ({id}) => {
   return (
      <OneBookWrapper>
         
      </OneBookWrapper>
   );
};

export default OneBookPage;